---

# 🛡️ 单元测试 (UT) 覆盖率门禁工具说明书

## 1. 概述

为确保项目代码质量，本项目实施 **UT 自动门禁机制**。在代码合并（Pull Request）过程中，系统会自动触发全量与增量覆盖率扫描，确保新增逻辑经过充分验证，防止存量代码质量退化。

---

## 2. 覆盖率维度说明

系统从两个维度对代码进行评估：

### 📈 全量覆盖率 (Total Coverage)

* **统计范围**：整个工程的所有可执行代码。
* **主要目的**：评估项目的整体技术债及测试完备度。
* **拦截要求**：作为参考指标，通常要求不低于 **10%**。

### 🚀 增量覆盖率 (Incremental Coverage) —— **核心门禁**

* **统计范围**：当前 PR/MR 中**新增或修改的代码行**。
* **获取方式**：通过 GitHub/GitCode API 实时获取 PR 的 `diff` 数据。
* **核心算法**：
* **排除项**：自动过滤注释、空行、单独的大括号、`package/import` 声明。
* **排除文件**：自动排除所有 `_test.go`、`Test*.java` 等测试辅助类文件。


* **拦截要求**：**增量覆盖率必须 ≥ 80%**，否则流水线将强制拦截。

---

## 3. 各语言技术栈与工具

本系统通过统一的脚本框架，针对不同语言采用最优的覆盖率收集策略：

| 语言 | 全量工具 | 增量方案 | 映射逻辑 |
| --- | --- | --- | --- |
| **Go** | `go test -cover` | **自研 Block 映射算法** | 将 Diff 行映射至 `coverage.out` 的 Statement 块，计算语句权重。 |
| **Java** | `Jacoco` | `diff-cover` | 对比 `jacoco.xml` 与 PR Patch，统计变更行的覆盖情况。 |
| **Python** | `pytest-cov` | `diff-cover` | 解析 `coverage.xml`，匹配 PR 变更区间。 |
| **Node.js** | `Vitest (v8)` | `diff-cover` | 利用 `clover.xml` 报告，确保 JS/TS 逻辑安全。 |

---

## 4. 拦截与准入规则 (Gatekeeping)

### 🚨 强制失败条件 (Exit 1)

1. **全量失败**：全量代码覆盖率低于 **10%**。
2. **增量未达标**：PR 修改部分的代码覆盖率低于 **80%**。
3. **解析异常**：由于语法错误导致覆盖率文件（如 `coverage.xml`, `coverage.out`）生成失败。

### ✅ 通过条件 (Exit 0)

1. 所有单元测试执行通过。
2. 增量覆盖率满足阈值要求。
3. 非 PR 场景（如主干直接推送）仅进行全量记录，不执行增量门禁拦截。

---

## 5. 增量解析细节 (实现原理)

### 🐹 Go 语言的“语句块”加权

Go 语言不简单按“行”统计。当 PR 修改了某一行，脚本会：

1. 在 `coverage.out` 中定位该行所属的 `Statement Block`。
2. 提取该 Block 包含的语句数（Weight）。
3. 若该 Block 命中（Hits > 0），则分子加上对应权重。这能更真实地反映 `if/else` 分支的覆盖。

### 📦 Node.js 的 Vitest 接入

* **环境**：使用 `jsdom` 环境模拟。
* **配置**：通过 `v8` 提供者收集原生覆盖率。
* **范围**：重点覆盖 `**/utils/**` 等核心逻辑目录，排除 `node_modules` 及静态资源。

---

## 6. 报告查看指南

每次扫描完成后，流水线控制台会输出 `PROJECT COVERAGE FINAL REPORT` 汇总表：

```text
====================================================
           PROJECT COVERAGE FINAL REPORT            
====================================================
| TYPE         | COVERAGE     | RESULT   |
----------------------------------------------------
| Go(Inc)      | 85.0%        | PASS     |
| Java(Inc)    | 72.4%        | FAIL     |
| Node.js(Inc) | -            | SKIP     |
====================================================

```

* **PASS**: 准入通过。
* **FAIL**: 需补充针对变更逻辑的测试用例。
* **SKIP**: 本次变更不涉及该语言的可执行代码。

---

**文档说明**：本工具链旨在通过自动化手段减少人工 Code Review 在逻辑测试覆盖上的心智负担。如有特殊情况需要绕过阈值，请联系架构组进行特殊报备。
