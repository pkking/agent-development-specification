apiVersion: v1
kind: Service
metadata:
  name: om-datacenter
  namespace: ai-test
spec:
  selector:
    app: om-datacenter
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
