# K8s Service 模板 — <<PROJECT_NAME>>
apiVersion: v1
kind: Service
metadata:
  name: <<PROJECT_NAME>>
  namespace: <<NAMESPACE>>
spec:
  selector:
    app: <<PROJECT_NAME>>
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
