# Skill: <skill-name>

> 项目自定义 skill 模板。复制本文件到 `skills/<your-skill>/SKILL.md`，按下方框架填写。

## 1. 名称

`<<PROJECT_NAME>>-<your-skill>`（与目录同名）

## 2. 触发词

AI agent 何时该调用本 skill：

- 用户消息含 `<触发短语 1>`
- 用户消息含 `<触发短语 2>`

## 3. 输入

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `arg1` | string | ✓ | 待填 |
| `arg2` | string | 可选 | 待填 |

## 4. 步骤

1. 待填
2. 待填
3. 待填

## 5. 输出

待填：期望产出格式（文件 / PR / 评论 / 配置变更等）。

## 6. 关联

- 团队 skill 规范：[`../../../../pipeline/project-layer/skills-spec.md`](../../../../pipeline/project-layer/skills-spec.md)
- 项目 CLAUDE：[`../../CLAUDE.md`](../../CLAUDE.md)
