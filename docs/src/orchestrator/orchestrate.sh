#!/usr/bin/env bash
# 多 agent 对抗编排
# 待填

set -uo pipefail
: "${SOURCE_REPO:?}"
: "${ISSUE_NUMBER:?}"
: "${BRANCH:?}"
: "${GH_TOKEN:?}"
: "${GITHUB_WORKSPACE:?}"
MAX_FIX_ROUNDS="${MAX_FIX_ROUNDS:-3}"

echo "[orchestrate] 待填: 4 agent 对抗主逻辑"
