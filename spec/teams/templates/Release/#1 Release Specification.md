# #1 [需求名称] 变更计划说明书

## 1. 变更概览

* **需求或版本发布Task链接**: **[TODO]**
* **变更内容概要**: **[TODO]**
* **关联测试报告链接**: **[TODO]** (如不涉及可不填)
* **开发责任人**: **[TODO]**_githubid & gitcodeid_
* **变更责任人**: **[TODO]**_githubid & gitcodeid_
* **变更时间**: **[TODO]**
* **变更等级**: **[TODO]**

* [X] **L1 (重大)**

> * 定义：涉及核心数据库 Schema 修改、全局配置中心变更或底层网络拓扑调整。
> * 管控要求：
> > * 1.值守要求：变更实行开发+运维1+1check。
> > * 2.强制灰度：严禁全量发布，必须包含分级发布步骤
> > * 3.预案验证：必须在测试环境完成 1:1 的回滚实操演练。
> > * 4.变更时间：强制选在业务低峰期。

* [ ] **L2 (普通)**

> * 定义：微服务配置变更、非核心插件发布、新开发服务上线。
> * 管控要求：
> > * 1.值守要求：变更实行开发+运维1+1check。
> > * 2.预案验证：必须在测试环境完成 1:1 的回滚实操演练。

* [ ] **L3 (轻微)**

> * 定义：文档更新、前端静态资源发布、不影响逻辑的配置微调和Bug修复。
> * 管控要求：
> > * 1.值守要求：变更可由开发通过发布流水线发布上线。

* **关联Issue汇总**

| 仓库 | IssueUrl | 标题 | 变更类型 | 关联业务 PR | PR 状态 | Issue 状态 |
|------|----------|------|---------|-----------|---------|-----------|
| **[TODO]** | #xx      | _Issue 标题_ | _新服务上线 / Bug修复 / 配置变更 等_ | _repo#PRNumber_ | _已合入 / open_ | _open / closed_ |

* **关联业务 PR**: **[TODO]** __


* **变更详细内容**

> **说明**：基于关联业务 PR (列出 backlog 仓以外的业务仓库 PR 链接，如 `org/repo#123`)的代码变更，汇总本次版本发布的实际变更内容。核心代码仓详细列出，部署脚本仓仅列关键配置。

### {repo1}#{PRNumber} — {PR标题}（核心代码仓）

| 变更文件 | 操作 | 变更说明 |
|---------|------|---------|
| `path/to/file` | _新增 / 修改 / 删除_ | _变更的功能说明_ |

### {repo2}#{PRNumber} — {PR标题}（部署脚本仓）

| 变更文件 | 操作 | 变更说明 |
|---------|------|---------|
| `path/to/file` | _新增 / 修改_ | _关键配置：namespace、镜像版本、chart 版本等_ |

---

## 2. 详细执行步骤

> **原则**：步骤必须可操作、原子化，且包含明确的执行人。

**执行步骤：** **[TODO]**

| 步骤    | 操作类型     | 操作内容描述 (具体命令或操作链接)                     | 预期结果                   | 执行人        |
|-------|----------|----------------------------------------|------------------------|------------|
| **1** | **配置变更** | _修改数据库微服务等关键配置文件。_                     | _配置变更成功_               | **[TODO]** |
| **2** | **灰度发布** | _应用新的 K8s Deployment 配置，设置灰度比例为 10%。_  | _新旧 Pod 共存，10% 流量转入新版_ | **[TODO]** |
| **3** | **线上观测** | _检查 Grafana 实时看板，确认 4xx/5xx 错误率无异常上涨。_ | _观测 15 分钟指标平稳_         | **[TODO]** |
| **4** | **全量发布** | _逐步提升灰度比例至 50%、100%。_                  | _旧版 Pod 优雅退出，全量运行新版_   | **[TODO]** |

---

## 3. 生产环境验证

> **参考方向**：变更后如何快速确认"系统是好的"且功能符合预期。

**验证方式：** **[TODO]**

_如：_

_**核心链路拨测**：通过监控系统对核心接口进行 1 分钟内的模拟请求，验证端到端通畅。_

_**日志特征匹配**：观察生产环境日志，确认无新增的 `ERROR` 堆栈，且新版本号已生效。_

_**业务指标闭环**：确认业务侧关键指标（如：构建任务触发、官网访问量）未出现阶梯式下滑。_


---

## 4. 回滚方案

> **原则**：回滚必须是受控且经过验证的，**禁止现场临时拼凑命令**。

> 出现以下任意一种情况，必须立即中断变更并启动回滚：
> 1. **指标异常**：错误率超过 5% 或 P99 延迟增加超过 50% 且持续 2 分钟。
> 2. **核心阻塞**：主要业务链路中断，且无法在 10 分钟内定位根因。
> 3. **不可抗力**：变更期间发生严重的云服务中断或网络分区。

> 参考：
> * **代码/版本回滚**：执行 `kubectl rollout undo` 命令或重新部署上一个稳定版本的镜像。
> * **配置回滚**：将配置中心（如 Nacos/Apollo）切换回上一个版本的快照。
> * **数据回滚**：执行数据库倒滚脚本或恢复快照（注意数据一致性评估）。

**回滚执行路径：**

**[TODO]**

| 步骤     | 回滚动作 | 执行命令/方式                                            | 预期结果                       |
|--------|------|----------------------------------------------------|----------------------------|
| **R1** | 配置还原 | **[TODO]**                                         | **[TODO]**   _配置项恢复至变更前状态_ |
| **R2** | 版本回退 | **[TODO]** _`kubectl rollout undo deployment/xxx`_ | **[TODO]**   _系统恢复旧版本镜像_   |

---
