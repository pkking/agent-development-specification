# K8s ConfigMap 模板 — <<PROJECT_NAME>>
apiVersion: v1
kind: ConfigMap
metadata:
  name: <<PROJECT_NAME>>-config
  namespace: <<NAMESPACE>>
data:
  APP_ENV: "preview"
  LOG_LEVEL: "info"
  # 在此添加项目所需非敏感配置
